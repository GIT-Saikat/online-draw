module.exports = {
  eslint: {
    // This will allow the build to complete even if there are ESLint errors
    ignoreDuringBuilds: true,
  },
  typescript: {
    // This will allow the build to complete even if there are TypeScript errors
    ignoreBuildErrors: true,
  }
};